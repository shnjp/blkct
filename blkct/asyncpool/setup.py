#!/usr/bin/env python
# -*- coding: utf-8 -*-

from setuptools import find_packages, setup

setup(
    name='asyncpool',
    version='0.1',
    packages=find_packages(exclude=['tests']),
    install_requires=[],
)
